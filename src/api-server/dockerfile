FROM node:alpine

WORKDIR /server

EXPOSE 3002

ARG REACT_APP_API_URL=http://localhost:3002
ARG REACT_APP_COLYSEUS_URL=ws://localhost:2567
ARG REACT_APP_CLIENT_URL=http://localhost:3000 

COPY package*.json ./
RUN npm i --force
COPY . . 

CMD ["npm", "start"]
