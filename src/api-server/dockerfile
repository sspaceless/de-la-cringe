FROM node:alpine

WORKDIR /server

EXPOSE 3002

ENV REACT_APP_COLYSEUS_URL=https://game-server.azurewebsites.net/
ENV REACT_APP_CLIENT_URL=http://localhost:3000 

COPY package*.json ./
RUN npm i --force
COPY . . 

CMD ["npm", "start"]
